# astridevette.github.io

Personal website for Astrid deRidder — content and learning expert.

Static HTML/CSS, no build step, no dependencies. To add a new piece of writing,
copy an `<article class="piece">` block in `index.html`.
